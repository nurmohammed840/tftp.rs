```rust
struct Request {
    filename: Text,
    mode: Text
}

enum Frame<'a> {
    Read(Request),
    Write(Request),
    Data { block: u16, bytes: &'a [u8] },
    Acknowledge(u16),
    ErrMsg { code: ErrorCode, msg: Text },
}
```